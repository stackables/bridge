# Built-in Tools Example

Demonstrates the built-in `std` namespace tools (`std.str.toUpperCase`, `std.str.toLowerCase`, `std.arr.find`) without any external APIs.

## Run

```bash
npx tsx server.ts
```
