# Weather Server Example

Chains two free APIs — geocoding (Nominatim) and weather (Open-Meteo) — with no API keys required.

## Files

- `Weather.graphql` — GraphQL schema
- `Weather.bridge` — tool definitions + field wiring
- `server.ts` — starts the server

## Run

```bash
npx tsx server.ts
```


