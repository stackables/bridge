type Query {
  geocode(search: String!, limit: Int): GeocodeResult
}

type GeocodeResult {
  search: String
  results: [GeocodeItem!]!
}

type GeocodeItem {
  name: String
  lat: Float
  lon: Float
}
