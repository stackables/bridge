type Query {
  format(text: String!): Formatted
  findEmployee(department: String!): Employee
}

type Formatted {
  original: String
  upper: String
  lower: String
}

type Employee {
  id: Int
  name: String
  department: String
}
